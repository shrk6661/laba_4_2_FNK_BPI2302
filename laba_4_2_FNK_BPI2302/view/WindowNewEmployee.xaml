<Window x:Class="laba_4_2_FNK_BPI2302.view.WindowNewEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сотрудник" 
        Height="450" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        
        <Label Grid.Row="0" Grid.Column="0" Content="Фамилия:" FontWeight="Bold" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" 
                 Margin="5" Padding="5"
                 Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"
                 Height="35" FontSize="14"/>

        
        <Label Grid.Row="1" Grid.Column="0" Content="Имя:" FontWeight="Bold" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" 
                 Margin="5" Padding="5"
                 Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"
                 Height="35" FontSize="14"/>

        
        <Label Grid.Row="2" Grid.Column="0" Content="Должность:" FontWeight="Bold" VerticalAlignment="Center"/>
        <ComboBox x:Name="CbRole" 
                  Grid.Row="2" Grid.Column="1" 
                  Margin="5"
                  DisplayMemberPath="NameRole"
                  SelectedValuePath="Id"
                  Height="35" FontSize="14"/>

        
        <Label Grid.Row="3" Grid.Column="0" Content="Дата рождения:" FontWeight="Bold" VerticalAlignment="Center"/>
        <DatePicker x:Name="ClBirthday" 
                    Grid.Row="3" Grid.Column="1" 
                    Margin="5"
                    SelectedDate="{Binding Birthday, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Height="35" FontSize="14"
                    DisplayDateStart="1950-01-01"
                    DisplayDateEnd="2010-12-31"
                    FirstDayOfWeek="Monday"/>

        
        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Bottom"
                    Margin="0,20,0,0">
            <Button Content="Сохранить" 
                    Width="120" Height="40" 
                    Margin="5" 
                    Click="BtnSave_Click"
                    Background="#4CAF50" 
                    Foreground="White"
                    FontWeight="Bold"
                    BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="Отмена" 
                    Width="120" Height="40" 
                    Margin="5" 
                    Click="BtnCancel_Click"
                    Background="#f44336" 
                    Foreground="White"
                    FontWeight="Bold"
                    BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>